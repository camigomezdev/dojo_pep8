default_stages: [commit, push]
default_language_version:
  python: python3.11
repos:
  - repo: https://github.com/pre-commit/mirrors-autopep8
    rev: "v2.0.2"
    hooks:
      - id: autopep8
        types: ["python"]
